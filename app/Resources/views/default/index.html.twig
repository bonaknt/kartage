{% extends 'base.html.twig' %}

{% block body %}
    <section id="portfolio">
      <div class="container">
        <h2 class="text-center">Catégories</h2>
        <hr class="star-primary">
        <div class="row">
        {% for category in categories %}
          <div class="col-lg-3 portfolio-item">
            <a class="portfolio-link" href="{{ path('categorie_show', { 'id': category.id }) }}" >
              <div style="background-color:#445567; width:100%; height:200px;">
              <span class="tspan">{{ category.title }} </span>
              </div>
            </a>
          </div>
          {% endfor %}
        </div>
      </div>
    </section>

{% endblock %}
